# Анализ данных хакатона и рекомендации

## ✅ Что было сделано

### 1. Перемещение данных
Все файлы из `/Users/kuralbekadilet475/Downloads/Хакатон 05.12` были перемещены в проект:

```
data/hackathon/
├── csv/
│   └── processed_data.csv          # Обработанные данные (6 строк, 27 столбцов)
├── documents/
│   ├── ffp_report/                  # Отчеты FFP
│   ├── final_report/                # Заключительные отчеты (Excel + Word)
│   ├── industrial_safety/           # ЗЭПБ
│   ├── questionnaire/               # Опросные листы
│   └── express_report/              # Экспресс отчеты
└── README.md                        # Документация
```

### 2. Анализ CSV данных

**Структура файла:**
- Формат: CSV с разделителем `;`
- Кодировка: Windows-1251
- Размер: 6 строк данных, 27 столбцов
- Координаты: Найдены в столбцах 20 (lat: 48.47-48.48) и 21 (lon: 57.66-57.67)
- Метод диагностики: "Магнитоскан" (MFL) в столбце 6
- Дефекты: "коррозия", "металлический объект", "сварной шов"
- Даты: 01.01.2025 в столбце 8

**Важные столбцы:**
- `#` - возможно, номер записи
- `основная` - основной параметр (6-10)
- `530`, `0-15`, `1000` - параметры измерений
- `Магнитоскан` - метод диагностики (MFL)
- `Unnamed: 9` - описание дефекта
- `Unnamed: 20`, `Unnamed: 21` - координаты (lat, lon)
- `Unnamed: 22` - высота (264.8-265.3)

## 📋 Что нужно сделать дальше

### 1. Конвертация CSV данных

Данные в текущем формате **не соответствуют** ожидаемому формату системы. Нужно создать скрипт конвертации.

**Текущий формат:**
- 27 столбцов с неясными названиями
- Смешанные данные (объекты + диагностика)

**Ожидаемый формат:**
- `Objects.csv`: object_id, object_name, object_type, pipeline_id, lat, lon, year, material
- `Diagnostics.csv`: diag_id, object_id, method, date, defect_found, defect_description, param1, param2, ml_label

**Действия:**
1. Создать `convert_hackathon_data.py` для преобразования
2. Маппинг столбцов:
   - `Unnamed: 20` → lat
   - `Unnamed: 21` → lon
   - `Магнитоскан` → method (MFL)
   - `Unnamed: 9` → defect_description
   - `530`, `0-15` → param1, param2
   - Даты из столбца 8 → date

### 2. Импорт в систему

После конвертации:
```bash
# Скопировать конвертированные файлы в data/
cp data/hackathon/csv/Objects.csv data/
cp data/hackathon/csv/Diagnostics.csv data/

# Импортировать через API
curl -X POST http://localhost:8000/api/v1/import
```

### 3. Работа с документами

**Текущее состояние:**
- Документы размещены в соответствующих папках
- Форматы: .docx, .doc, .xlsx

**Возможные действия:**
1. **Парсинг документов** (опционально):
   - Извлечение метаданных из Word/Excel
   - Связь документов с объектами диагностики
   - Хранение ссылок в БД

2. **Хранение:**
   - Документы остаются в файловой системе
   - Можно добавить таблицу `documents` в БД для метаданных

## 🔍 Детальный анализ данных

### Найденные данные:
- **6 записей диагностики** с координатами в районе 48.47°N, 57.66°E
- **Метод:** Магнитоскан (MFL - Magnetic Flux Leakage)
- **Дефекты:** коррозия, металлические объекты, сварные швы
- **Параметры:** различные числовые значения (530, 0-15, 1000, и др.)

### Проблемы:
1. **Неясная структура:** многие столбцы без названий (Unnamed)
2. **Смешанные данные:** объекты и диагностика в одном файле
3. **Мало данных:** только 6 записей
4. **Отсутствие:** object_id, pipeline_id, object_type

## 💡 Рекомендации

### Приоритет 1: Конвертация данных
Создать скрипт `convert_hackathon_data.py`, который:
1. Читает `processed_data.csv`
2. Извлекает объекты (по координатам или другим признакам)
3. Создает `Objects.csv` с объектами
4. Создает `Diagnostics.csv` с диагностическими данными
5. Связывает диагностику с объектами

### Приоритет 2: Валидация
Проверить:
- Корректность координат
- Соответствие методов диагностики (MFL → MFL)
- Формат дат
- Наличие обязательных полей

### Приоритет 3: Дополнение данных
Если данных недостаточно:
- Добавить pipeline_id (можно сгенерировать, например, "HACK-01")
- Добавить object_type (скорее всего "pipeline_section")
- Добавить year и material (если известны)

## 🛠️ Инструменты

- ✅ `analyze_data.py` - анализ структуры (готов)
- ⏳ `convert_hackathon_data.py` - конвертация (нужно создать)
- ✅ `README.md` - документация (готов)

## 📝 Пример конвертации

Примерная логика:
```python
# Для каждой строки в processed_data.csv:
# 1. Создать объект (если еще не создан для этих координат)
object = {
    'object_id': generate_id(),
    'object_name': f'HACK-Section-{i}',
    'object_type': 'pipeline_section',
    'pipeline_id': 'HACK-01',
    'lat': row['Unnamed: 20'],
    'lon': row['Unnamed: 21'],
    'year': None,  # неизвестно
    'material': None  # неизвестно
}

# 2. Создать диагностику
diagnostic = {
    'diag_id': generate_id(),
    'object_id': object['object_id'],
    'method': 'MFL',  # из "Магнитоскан"
    'date': '2025-01-01',  # из столбца 8
    'defect_found': row['Unnamed: 9'] is not None,
    'defect_description': row['Unnamed: 9'],
    'param1': convert_to_float(row['530']),
    'param2': convert_to_float(row['0-15']),
    'ml_label': None  # будет предсказано ML моделью
}
```

## ✅ Итог

**Сделано:**
- ✅ Данные перемещены в проект
- ✅ Создана структура директорий
- ✅ Проведен анализ данных
- ✅ Создана документация

**Нужно сделать:**
- ⏳ Создать скрипт конвертации
- ⏳ Конвертировать данные
- ⏳ Импортировать в систему
- ⏳ Проверить корректность импорта


