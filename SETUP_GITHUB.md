# 🚀 Настройка GitHub для IntegrityOS

## Шаг 1: Настройка Git (имя и email)

```bash
# Настройте ваше имя и email для коммитов
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Или только для этого репозитория:
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `integrityos`
   - **Description**: `Система мониторинга магистральных трубопроводов с AI/ML аналитикой`
   - **Visibility**: Public (или Private)
   - **НЕ** создавайте README, .gitignore или LICENSE (они уже есть)
4. Нажмите **"Create repository"**

## Шаг 3: Добавление remote и загрузка

После создания репозитория GitHub покажет вам URL. Используйте его:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/integrityos.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/integrityos.git

# Проверьте remote
git remote -v

# Загрузите код на GitHub
git push -u origin main
```

## Шаг 4: Настройка репозитория на GitHub

После загрузки:

1. **Добавьте описание** в настройках репозитория
2. **Добавьте Topics (теги)**:
   - `pipeline-monitoring`
   - `machine-learning`
   - `fastapi`
   - `nextjs`
   - `typescript`
   - `python`
   - `ml`
   - `ai`
   - `hackathon`
   - `promtech`

3. **Проверьте README** — он должен отображаться на главной странице

## Шаг 5: Исправление автора первого коммита (опционально)

Если нужно изменить автора первого коммита:

```bash
git commit --amend --author="Ваше Имя <your.email@example.com>" --no-edit
git push -f origin main  # ⚠️ Только если репозиторий новый и пустой!
```

## 🔐 Аутентификация

### HTTPS (рекомендуется для начинающих)
- При первом push GitHub запросит логин и пароль
- Для безопасности используйте [Personal Access Token](https://github.com/settings/tokens) вместо пароля

### SSH (рекомендуется)
1. Создайте SSH ключ (если нет):
   ```bash
   ssh-keygen -t ed25519 -C "your.email@example.com"
   ```
2. Добавьте ключ в GitHub: Settings → SSH and GPG keys
3. Используйте SSH URL: `git@github.com:YOUR_USERNAME/integrityos.git`

## ✅ Проверка

После загрузки проверьте:
- [ ] Все файлы загружены
- [ ] README.md отображается корректно
- [ ] .gitignore работает (venv, node_modules не видны)
- [ ] Лицензия отображается
- [ ] Описание и теги добавлены

## 🆘 Решение проблем

### Ошибка "Repository not found"
- Проверьте правильность URL
- Убедитесь, что репозиторий создан на GitHub
- Проверьте права доступа

### Ошибка аутентификации
- Используйте Personal Access Token для HTTPS
- Или настройте SSH ключи

### Конфликт при push
```bash
# Если на GitHub уже есть файлы:
git pull origin main --allow-unrelated-histories
# Разрешите конфликты, затем:
git push -u origin main
```

---

**Готово! 🎉** Ваш проект теперь на GitHub!

