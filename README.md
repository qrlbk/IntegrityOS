# 🛢️ IntegrityOS

> **Система мониторинга магистральных трубопроводов с AI/ML аналитикой**

[![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-009688?style=flat-square&logo=fastapi)](https://fastapi.tiangolo.com/)
[![Next.js](https://img.shields.io/badge/Next.js-14.0-000000?style=flat-square&logo=next.js)](https://nextjs.org/)
[![Python](https://img.shields.io/badge/Python-3.11+-3776AB?style=flat-square&logo=python)](https://www.python.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.3-3178C6?style=flat-square&logo=typescript)](https://www.typescriptlang.org/)

**IntegrityOS** — это современная платформа для мониторинга и анализа состояния магистральных трубопроводов. Система автоматически классифицирует дефекты с помощью машинного обучения, визуализирует данные на интерактивной карте и генерирует отчеты для принятия решений.

> 🏆 **Награда:** 🥉 **3-е место** на **PromTechHackathon 2025**

---

## ✨ Основные возможности

### 🗺️ Интерактивная визуализация
- **Карта Казахстана** с реальной геометрией трубопроводов (MT-01, MT-02, MT-03)
- **Цветовая индикация** критичности объектов (красный/желтый/зеленый)
- **Кластеризация маркеров** для работы с большими объемами данных
- **Фильтрация** по методам диагностики, датам, параметрам

### 🤖 AI/ML аналитика
- **Автоматическая классификация** дефектов (normal/medium/high)
- **Random Forest модель** для предсказания критичности
- **AI мониторинг** качества ML модели через OpenAI API
- **Rule-based логика** как fallback при отсутствии обученной модели

### 📊 Дашборд и аналитика
- **Статистика** по методам диагностики и критичности
- **Топ-5 рисковых объектов** с детальной информацией
- **Динамика дефектов** по годам
- **Распределение** дефектов по методам контроля

### 📄 Отчетность
- **Генерация наряд-допусков** в формате PDF
- **Полные отчеты** с картами и статистикой
- **История диагностик** для каждого объекта
- **Экспорт данных** для дальнейшего анализа

### 🔄 Импорт и обработка данных
- **Импорт CSV/XLSX** файлов с валидацией
- **Автоматическое определение** типа файла (Objects/Diagnostics)
- **Обработка ошибок** с детальным логированием
- **Батчевая обработка** для больших объемов данных

---

## 🚀 Быстрый старт

### Предварительные требования

- **Python** 3.11+
- **Node.js** 18+
- **Docker** и Docker Compose (для PostgreSQL/Redis)
- **Git**

### Установка и запуск

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd integrityos
```

#### 2. Запуск инфраструктуры (PostgreSQL + Redis)

```bash
docker-compose up -d
```

#### 3. Настройка Backend

```bash
cd backend

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения (опционально)
cat > .env << EOF
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/integrityos
CORS_ORIGINS=["http://localhost:3000"]
OPENAI_API_KEY=your-key-here  # Опционально, для AI функций
EOF

# Применение миграций
alembic upgrade head

# Запуск сервера
uvicorn app.main:app --reload
```

Backend будет доступен на `http://localhost:8000`

#### 4. Генерация тестовых данных

В новом терминале:

```bash
cd scripts
python generate_data.py
```

Это создаст файлы `data/Objects.csv` и `data/Diagnostics.csv`

#### 5. Импорт данных

```bash
# Импорт через API
curl -X POST http://localhost:8000/api/import

# Или через Swagger UI
# Откройте http://localhost:8000/docs
```

#### 6. Настройка Frontend

В новом терминале:

```bash
cd frontend

# Установка зависимостей
npm install

# Создание файла окружения
cat > .env.local << EOF
NEXT_PUBLIC_API_URL=http://localhost:8000
EOF

# Запуск dev сервера
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

---

## 🛠️ Технологический стек

### Frontend
- **Next.js 14** (App Router) — React фреймворк
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **Leaflet** / **react-leaflet** — интерактивные карты
- **Recharts** — графики и визуализация
- **Framer Motion** — анимации
- **Radix UI** — UI компоненты

### Backend
- **FastAPI** — современный Python веб-фреймворк
- **SQLAlchemy** — ORM для работы с БД
- **Alembic** — миграции базы данных
- **Pydantic** — валидация данных
- **Pandas** — обработка CSV/XLSX

### База данных
- **SQLite** — для разработки (MVP)
- **PostgreSQL** — для production (готово к миграции)
- **Redis** — кэширование и очереди

### ML/AI
- **Scikit-learn** — Random Forest Classifier
- **OpenAI API** — мониторинг и анализ ML модели
- **MLflow** — трекинг ML экспериментов
- **Evidently** — мониторинг дрифта данных
- **Optuna** — оптимизация гиперпараметров

### Инструменты разработки
- **Docker** / **Docker Compose** — контейнеризация
- **Git** — контроль версий
- **Alembic** — миграции БД

---

## 📡 API Endpoints

### Объекты
- `GET /api/objects` — список объектов с фильтрацией
- `GET /api/objects/{object_id}` — детали объекта

### Диагностика
- `GET /api/diagnostics` — список диагностик
- `GET /api/diagnostics/{object_id}` — диагностики для объекта

### Импорт
- `POST /api/import` — импорт CSV/XLSX файлов из папки `data/`

### Аналитика
- `GET /api/analytics/stats-summary` — общая статистика
- `GET /api/analytics/methods-distribution` — распределение по методам
- `GET /api/analytics/criticality-distribution` — распределение по критичности
- `GET /api/analytics/top-risks` — топ-5 рисковых объектов
- `GET /api/analytics/defects-timeline` — динамика дефектов

### ML/AI
- `GET /api/ml/monitor/metrics` — метрики ML модели
- `GET /api/ml/monitor/analysis` — AI анализ модели
- `POST /api/ml/train` — обучение модели
- `POST /api/ai/chat` — чат с AI Assistant (RAG)

### Наряд-допуски
- `POST /api/work-permits` — создание наряд-допуска
- `GET /api/work-permits/{permit_id}` — получение наряд-допуска

**Полная документация API:** `http://localhost:8000/docs` (Swagger UI)

---

## 📁 Структура проекта

```
integrityos/
├── backend/                 # FastAPI приложение
│   ├── app/
│   │   ├── api/            # API endpoints
│   │   │   └── v1/        # Версия API
│   │   ├── core/          # Конфигурация, БД, ML модель
│   │   ├── models/        # SQLAlchemy модели
│   │   ├── schemas/       # Pydantic схемы
│   │   └── services/      # Бизнес-логика
│   ├── alembic/           # Миграции БД
│   ├── requirements.txt   # Python зависимости
│   └── main.py            # Точка входа
│
├── frontend/              # Next.js приложение
│   ├── app/               # Страницы (App Router)
│   ├── components/         # React компоненты
│   │   ├── map/           # Компоненты карты
│   │   ├── charts/        # Графики
│   │   ├── dashboard/     # Дашборд
│   │   └── print/         # Отчеты
│   ├── contexts/          # React контексты
│   ├── lib/               # Утилиты и API клиент
│   └── package.json       # Node зависимости
│
├── data/                  # CSV/XLSX файлы с данными
│   ├── Objects.csv
│   └── Diagnostics.csv
│
├── scripts/               # Утилиты
│   └── generate_data.py   # Генератор тестовых данных
│
├── docker-compose.yml     # Docker конфигурация
├── README.md              # Этот файл
├── QUICK_START.md         # Детальный гайд по запуску
├── ARCHITECTURE.md        # Архитектура системы
└── HOW_SYSTEM_WORKS.md   # Как работает система
```

---

## 🎯 Основные функции

### Импорт данных
Система поддерживает импорт данных из CSV/XLSX файлов:
- Автоматическое определение типа файла (Objects/Diagnostics)
- Валидация данных с детальным логированием ошибок
- Батчевая обработка для больших объемов
- Автоматическое определение критичности через ML или правила

### Картографическая визуализация
- Интерактивная карта на основе Leaflet
- Реальная геометрия трубопроводов
- Цветовая индикация критичности
- Кластеризация маркеров для производительности
- Фильтрация по множеству параметров

### ML классификация
- **Random Forest** модель для предсказания критичности
- Автоматическое обучение при наличии размеченных данных
- Rule-based логика как fallback
- Мониторинг качества модели через AI

### Дашборд
- Статистика по методам диагностики
- Распределение по критичности
- Топ-5 рисковых объектов
- Динамика дефектов по годам

### Отчетность
- Генерация наряд-допусков в PDF
- Полные отчеты с картами и статистикой
- История диагностик для каждого объекта

---

## 🔧 Разработка

### Генерация тестовых данных

```bash
cd scripts
python generate_data.py
```

Создаст файлы:
- `data/Objects.csv` — ~400 объектов
- `data/Diagnostics.csv` — ~1800 записей диагностики

### Миграции базы данных

```bash
cd backend

# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

### Запуск тестов

```bash
# Backend (если есть тесты)
cd backend
pytest

# Frontend
cd frontend
npm run lint
```

---

## 📚 Дополнительная документация

- **[QUICK_START.md](./QUICK_START.md)** — детальное руководство по запуску
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** — архитектура системы
- **[HOW_SYSTEM_WORKS.md](./HOW_SYSTEM_WORKS.md)** — как работает система
- **[ЧЕКЛИСТ_СООТВЕТСТВИЯ_ТЗ.md](./ЧЕКЛИСТ_СООТВЕТСТВИЯ_ТЗ.md)** — соответствие техническому заданию

---

## 🤖 AI Assistant (Опционально)

Для использования AI Assistant с OpenAI API:

1. Создайте файл `.env` в папке `backend/`:
```bash
cd backend
cp .env.example .env  # если есть пример
```

2. Добавьте ваш OpenAI API ключ:
```env
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

3. Получите ключ: https://platform.openai.com/api-keys

4. Перезапустите сервер

Без ключа AI Assistant будет работать в упрощенном режиме.

Подробнее: `backend/README_AI.md`

---

## 🐛 Решение проблем

### PostgreSQL не запускается
```bash
docker-compose logs postgres
docker-compose down
docker-compose up -d postgres
```

### Ошибки импорта CSV
- Проверьте формат CSV (должен быть UTF-8)
- Убедитесь, что все обязательные колонки присутствуют
- Проверьте типы данных (object_id и diag_id должны быть числами)

### Leaflet карта не отображается
- Убедитесь, что компонент загружается динамически (ssr: false)
- Проверьте консоль браузера на ошибки
- Проверьте API: `curl http://localhost:8000/api/objects`

### CORS ошибки
- Убедитесь, что `CORS_ORIGINS` в `.env` включает `http://localhost:3000`
- Проверьте, что backend запущен на правильном порту

---

## 📄 Лицензия

Разработано для **PromTechHackathon 2025**

---

## 👥 Команда

Проект разработан командой для хакатона PromTechHackathon 2025.

---

## 🚀 Планы развития

- [ ] Интеграция с IoT датчиками для мониторинга в реальном времени
- [ ] Мобильное приложение для полевых работ
- [ ] Расширенная аналитика и предиктивное моделирование
- [ ] Интеграция с внешними системами (SAP, 1C)
- [ ] WebSocket для real-time обновлений
- [ ] Расширенная система отчетности

---

**Видим сквозь землю, предотвращаем аварии** 🛢️✨
